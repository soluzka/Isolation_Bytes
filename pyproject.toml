[tool.pylint.messages_control]
disable = ["E0102"]

[tool.flake8]
ignore = ["F811"]

[project]
name = "my-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "soluzka", email = "soluzka@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
my-mcp-server = "my_mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
